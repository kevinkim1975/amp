# 프로젝트 맥락 분석 요약
## Boris Cherny 관점의 단계별 파악 결과

---

## 📋 프로젝트 개요

**프로젝트명**: `amp` (AutoMed Proposal Generator)  
**목적**: 서일메디컬그룹을 위한 19페이지 마케팅 제안서 웹 애플리케이션  
**기술 스택**: Next.js 15 + TypeScript 5.9.3 + Tailwind CSS  
**배포**: Vercel (GitHub 연동 자동 배포)  
**현재 상태**: Phase 2 진행 중 (19페이지 중 5페이지 완료)

---

## 🎯 핵심 발견사항

### 1. 타입 시스템 (⭐⭐⭐⭐⭐)

**강점:**
- ✅ **타입을 먼저 정의**: `src/types/index.ts`가 모든 컴포넌트보다 먼저 존재
- ✅ **Literal Types 활용**: `SectionId = 1 | 2 | ... | 19`로 런타임 오류 방지
- ✅ **readonly 사용**: 불변성 보장 (`readonly id`, `readonly type`)
- ✅ **Discriminated Union**: `SectionType`으로 타입 안전성 확보

**Boris Cherny 원칙 준수도**: ⭐⭐⭐⭐⭐ (완벽)

### 2. 컴포넌트 아키텍처 (⭐⭐⭐⭐☆)

**구조:**
```
src/app/page.tsx              # 상태 관리 (currentSection)
src/components/layout/        # TopbarHeader, ProgressFooter
src/components/sections/      # 19개 섹션 컴포넌트
```

**강점:**
- ✅ **관심사 분리**: Layout vs Content 명확히 분리
- ✅ **Container-Presenter 패턴**: `SectionContainer`가 라우팅 담당
- ✅ **단일 책임 원칙**: 각 섹션이 하나의 페이지만 담당

**개선 가능성:**
- ⚠️ `SectionComponents` 타입이 `Record<number, ...>` → `Record<SectionId, ...>`로 개선 가능

### 3. 디자인 시스템 (⭐⭐⭐⭐⭐)

**Single Source of Truth:**
- `docs/design-guide.md` → 디자인 스펙 정의
- `tailwind.config.ts` → Tailwind 설정
- `src/app/globals.css` → CSS Variables

**강점:**
- ✅ **중앙 집중식 관리**: 색상, 타이포그래피, 스페이싱이 한 곳에 정의
- ✅ **일관성**: 모든 컴포넌트가 동일한 디자인 언어 사용
- ✅ **타입 안전성**: Tailwind IntelliSense로 자동완성

### 4. 데이터 구조 (⭐⭐⭐☆☆)

**현재 상태:**
- YAML 파일 (`data/seoil-medical.yaml`) 존재하나 실제 사용 안 함
- 각 섹션 컴포넌트가 자체 데이터를 하드코딩
- 예: `Section05Keywords.tsx` 내부에 `keywordsData` 상수

**문제점:**
- ⚠️ **DRY 위반**: YAML과 TypeScript 코드 중복
- ⚠️ **유지보수 어려움**: 데이터 변경 시 두 곳 수정 필요

**개선 방향:**
- YAML → TypeScript 자동 변환 (빌드 타임)
- 타입 안전한 데이터 사용

### 5. 상태 관리 (⭐⭐⭐⭐☆)

**구현:**
```typescript
const [currentSection, setCurrentSection] = useState<SectionId>(1);
const sectionRefs = useRef<(HTMLElement | null)[]>([]);
```

**강점:**
- ✅ **타입 안전한 상태**: `SectionId` literal type 사용
- ✅ **useCallback 메모이제이션**: 불필요한 리렌더링 방지
- ✅ **Intersection Observer**: 네이티브 API 활용 (의존성 최소화)

**개선 가능성:**
- ⚠️ 타입 단언 (`as SectionId`) → 타입 가드로 개선 가능
- ⚠️ 에러 처리 부족 (null 체크 추가 필요)

---

## 📊 프로젝트 구조 분석

### 파일 구조
```
amp/
├── data/
│   └── seoil-medical.yaml          # 참고용 데이터 (실제 사용 안 함)
├── docs/
│   ├── PRD.md                      # 제품 요구사항
│   ├── design-guide.md             # 디자인 시스템
│   ├── code-guide.md               # 코딩 가이드라인
│   ├── IA.md                       # 정보 아키텍처
│   └── user-journey.md             # 사용자 여정
├── src/
│   ├── app/
│   │   ├── layout.tsx              # Root Layout
│   │   ├── page.tsx                # Main Page (상태 관리)
│   │   └── globals.css             # CSS Variables
│   ├── components/
│   │   ├── layout/
│   │   │   ├── TopbarHeader.tsx    # 상단 헤더 (64px)
│   │   │   └── ProgressFooter.tsx  # 하단 푸터 (80px)
│   │   └── sections/
│   │       ├── SectionContainer.tsx # 섹션 라우터
│   │       ├── Section01Cover.tsx
│   │       ├── Section02TOC.tsx
│   │       └── ... (19개 섹션)
│   ├── types/
│   │   └── index.ts                # 타입 정의 (Single Source of Truth)
│   └── lib/
│       └── utils.ts               # 유틸리티 함수 (cn)
├── package.json
├── tsconfig.json                   # TypeScript 설정 (strict mode)
├── tailwind.config.ts              # Tailwind 설정
└── TASK.md                         # 작업 진행 상황
```

### 섹션 구성 (19페이지)

| ID | 섹션명 | 타입 | 상태 |
|----|--------|------|------|
| 1 | 표지 | cover | ✅ 완료 |
| 2 | 목차 | toc | ✅ 완료 |
| 3 | 서일메디컬 현황 진단 | content | ✅ 완료 |
| 4 | 마케팅 채널 | content | ✅ 완료 |
| 5 | 바이럴 키워드 분석 | content | ✅ 완료 |
| 6 | 경쟁 구도 개요 | content | ⬜ |
| 7 | 재건성형 경쟁사 | content | ⬜ |
| 8 | 미용성형 경쟁사 | content | ⬜ |
| 9 | 미용성형 비교 분석 | content | ⬜ |
| 10 | 서일메디컬 현황 진단 | content | ⬜ |
| 11 | 포지셔닝 (재건성형) | content | ⬜ |
| 12 | 포지셔닝 (미용성형) | content | ⬜ |
| 13 | 핵심 문제점 진단 | content | ⬜ |
| 14 | 전후사진 확보 전략 | content | ⬜ |
| 15 | 채널별 전략 | content | ⬜ |
| 16 | 마케팅 예산안 | content | ⬜ |
| 17 | 향후 확장 방향 | content | ⬜ |
| 18 | 요약 및 권장사항 | content | ⬜ |
| 19 | 감사합니다 | closing | ✅ 완료 |

---

## 🔍 Boris Cherny 원칙 준수도 평가

| 원칙 | 준수도 | 평가 |
|------|--------|------|
| 타입 먼저 정의 | ⭐⭐⭐⭐⭐ | 완벽 |
| 불변성 보장 | ⭐⭐⭐⭐☆ | readonly 사용 |
| 함수형 프로그래밍 | ⭐⭐⭐⭐☆ | useCallback 활용 |
| 명확한 네이밍 | ⭐⭐⭐⭐⭐ | 의미 있는 이름 |
| 단일 책임 원칙 | ⭐⭐⭐⭐☆ | 컴포넌트 분리 |
| DRY 원칙 | ⭐⭐⭐☆☆ | 데이터 중복 존재 |

**전체 평가: ⭐⭐⭐⭐☆ (4.5/5)**

---

## ✅ 즉시 개선 가능한 항목

### 1. SectionContainer 타입 안전성 개선

**현재:**
```typescript
const SectionComponents: Partial<Record<number, React.ComponentType>> = {
  // ...
};
```

**개선:**
```typescript
const SectionComponents: Partial<Record<SectionId, React.ComponentType>> = {
  // ...
};
```

### 2. 타입 단언을 타입 가드로 변경

**현재:**
```typescript
const nextSection = (currentSection + 1) as SectionId;
```

**개선:**
```typescript
const isValidSectionId = (id: number): id is SectionId => {
  return id >= 1 && id <= TOTAL_SECTIONS;
};

if (isValidSectionId(currentSection + 1)) {
  const nextSection = currentSection + 1;
  scrollToSection(nextSection);
}
```

### 3. 에러 처리 추가

**현재:**
```typescript
const targetRef = sectionRefs.current[sectionId - 1];
if (targetRef) {
  targetRef.scrollIntoView({ behavior: 'smooth', block: 'start' });
}
```

**개선:**
```typescript
const targetRef = sectionRefs.current[sectionId - 1];
if (!targetRef) {
  console.warn(`Section ${sectionId} not found`);
  return;
}
targetRef.scrollIntoView({ behavior: 'smooth', block: 'start' });
```

---

## 📝 중기 개선 방향

### 1. 데이터 동기화
- YAML → TypeScript 자동 변환 스크립트 작성
- 빌드 타임에 타입 안전한 데이터 생성

### 2. 테스트 추가
- Jest + React Testing Library 설정
- 단위 테스트 및 통합 테스트 작성

### 3. 성능 최적화
- Lazy Loading 적용 (섹션 컴포넌트)
- Next.js Image 컴포넌트 사용 (향후 이미지 추가 시)

---

## 🎓 학습 포인트

### 잘 설계된 부분
1. **타입 시스템**: Literal Types와 Discriminated Union 활용
2. **디자인 시스템**: Single Source of Truth 원칙 준수
3. **컴포넌트 구조**: 명확한 관심사 분리

### 개선이 필요한 부분
1. **데이터 중복**: YAML과 TypeScript 코드 동기화 필요
2. **타입 안전성**: 타입 단언을 타입 가드로 개선
3. **에러 처리**: null 체크 및 경고 로그 추가

---

## 📚 참고 문서

- **상세 분석**: `PROJECT_ANALYSIS.md` (영문)
- **작업 진행**: `TASK.md`
- **디자인 가이드**: `docs/design-guide.md`
- **코딩 가이드**: `docs/code-guide.md`

---

**작성일**: 2026-01-13  
**분석 기준**: Boris Cherny의 "Programming TypeScript" 원칙  
**프로젝트 상태**: Phase 2 진행 중 (19페이지 중 5페이지 완료)
